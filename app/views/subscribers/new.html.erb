<h1>New subscriber</h1>

<% form_for(@subscriber) do |f| %>
	<%= f.error_messages %>
	<%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', subscribers_path %>

<%= focus_field :subscriber_name %>
