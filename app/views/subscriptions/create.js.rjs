page[:subscriptions].replace_html :partial => "lists/subscriptions", 
                                  :locals => {:subscriptions => @subscriptions, 
                                  :available_subscribers => @available_subscribers }

page[:hidden_subscription_form].replace_html  :partial => 'lists/hidden_subscription_form', 
                                              :locals => { :subscription => Subscription.new, :list => @list}