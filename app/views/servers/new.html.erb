<h2>New server</h2>

<% form_for(@server) do |f| %>
	<%= f.error_messages %>
	<%= render :partial => 'form', :locals => { :f => f } %>
	<p>
    	<%= f.submit "Create Server" %>
  	</p>
<% end %>

<%= link_to 'Back', servers_path %>

<%= focus_field :server_name %>
