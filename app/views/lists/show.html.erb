<p class="top-right-links">
<% if current_user.admin? %>
      <%= link_to 'Edit List', edit_list_path(@list) %> |
	  <%= link_to 'Delete List', list_path(@list), :method => :delete, :confirm => "Are you sure?" %>
<% else %>
	<% if @list.subscribers.include?(current_user) %>
    	You are subscribed to this list
    	<%= link_to 'Unsubscribe', unsubscribe_list_path(@list) %>
  	<% else %>
    	<%= link_to 'Subscribe', subscribe_list_path(@list) %>
  	<% end %>
<% end %>
</p>
<h2><%=h @list.name %> - <%=h @list.email %></h2>
<p><%=h @list.description %></p>

<table class="subscribers-topics">
	<tr>
		<% if current_user.admin? %>
		<td>
			<h3>Subscribers</h3>

<div id="hidden_subscription_form">
	<%= render :partial => 'lists/hidden_subscription_form', :locals => { :subscription => @subscription, :list => @list } %>
</div>

<div id="subscriptions">
	<%= render :partial => 'lists/subscriptions', :locals => { :subscriptions => @list.subscriptions, :available_subscribers => @available_subscribers } %>
</div>
		</td>
		<% end %>
		<td>

<h3>Recent Topics</h3>
<% if @list.topics.present? %>
	<% @list.topics.each do |topic| %>
		<p><%= link_to h(topic.name), list_topic_path(@list, topic) %></p>
	<% end %>
<% else %>
	<p>There are no recent topics for this list.</p>
<% end %>
<%= link_to h("[All Topics]"), list_topics_path(@list) %>
		</td>
	</tr>
</table>
